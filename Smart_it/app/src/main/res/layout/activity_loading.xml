<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#F0F0F0"
    android:padding="32dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- 상단 여백 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- 돋보기와 애니메이션 (절대 좌표 방식) -->
    <FrameLayout
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="20dp">

        <!-- 돋보기 이미지 (통합된 돋보기) -->
        <ImageView
            android:id="@+id/magnifierIcon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:src="@drawable/magnifier_icon"
            android:scaleType="centerInside" />

        <!-- 점 #1 (돋보기 원 중심에 정확히 배치) -->
        <View
            android:id="@+id/dot1"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_gravity="center"
            android:layout_marginStart="-8dp"
            android:layout_marginTop="-8dp"
            android:background="@drawable/loading_dot"
            android:alpha="0.3" />

        <!-- 점 #2 (중앙) -->
        <View
            android:id="@+id/dot2"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_gravity="center"
            android:layout_marginStart="-20dp"
            android:layout_marginTop="-8dp"
            android:background="@drawable/loading_dot"
            android:alpha="0.3" />

        <!-- 점 #3 (오른쪽) -->
        <View
            android:id="@+id/dot3"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_gravity="center"
            android:layout_marginStart="4dp"
            android:layout_marginTop="-8dp"
            android:background="@drawable/loading_dot"
            android:alpha="0.3" />

    </FrameLayout>

    <!-- 메인 타이틀 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="신뢰도 검사 진행 중"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="12dp" />

    <!-- 상태 메시지 -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔍 뉴스 신뢰도 평가 중..."
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- 예상 시간 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="15초 ~ 30초 예상"
        android:textSize="14sp"
        android:textColor="@color/white"
        android:background="@drawable/time_estimate_background"
        android:paddingHorizontal="20dp"
        android:paddingVertical="8dp"
        android:layout_marginBottom="32dp" />

    <!-- 하단 여백 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>